export type TodoObject = {
  id: string;
  label: string;
  isCompleted: boolean;
};
export type ExpenseObject = {
  id: string;
  note: string;
  amt: string;
  postive: boolean;
};
export type NotesObject = {
  id: string;
  text: string;
};
